#first need to make sure we have star_search.jar 
javac *.java
jar cfe fuel_test.jar Main *.class

rm *results.csv

for i in {0..30}; do
    java -jar star_search.jar scenario${i}.csv > scenario${i}_results.csv
done

java -jar fuel_test.jar 0 30 > result.log